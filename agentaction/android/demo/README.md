# Android Demo

使用 Android Studio 打开运行，或执行：

```bash
cd agentaction/android/demo
./gradlew :app:assembleDebug
```

Demo 使用 Mock 发送器，请将 `MainActivity` 中的发送器 Lambda 替换为 ZEGO Express 房间通道消息集成即可对接真实流量。

> 英文版本请见 [README.en.md](./README.en.md)。
