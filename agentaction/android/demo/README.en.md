# Android Demo

Run with Android Studio or:

```bash
cd agentaction/android/demo
./gradlew :app:assembleDebug
```

The demo uses a mock sender. Replace the sender lambda in `MainActivity` with ZEGO Express room channel message integration for real traffic.

> Chinese version: [README.md](./README.md)
