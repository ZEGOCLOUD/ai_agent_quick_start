# Flutter Demo

该目录是可运行的 Flutter Demo 工程。运行方式：

```bash
cd agentaction/flutter/demo
flutter pub get
flutter run
```

Demo 使用 Mock 发送器，并打印生成的房间通道消息参数。实际接入时请将 Mock 发送器替换为 ZEGO Express 房间通道消息集成。

> 英文版本请见 [README.en.md](./README.en.md)。
