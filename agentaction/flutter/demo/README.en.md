# Flutter Demo

This folder is a runnable Flutter demo project. To run it:

```bash
cd agentaction/flutter/demo
flutter pub get
flutter run
```

The demo uses a mock sender and logs the generated room channel message params. Replace the mock sender with ZEGO Express room channel message integration for real traffic.

> Chinese version: [README.md](./README.md)
