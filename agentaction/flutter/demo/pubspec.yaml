name: zego_ai_agent_action_demo
description: Demo for ZEGO AI Agent Action Flutter suite.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  zego_ai_agent_action:
    path: ../agentaction
  zego_express_engine: ^3.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
